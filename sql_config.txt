//Create DB

CREATE DATABASE UDM;
USE UDM;
CREATE TABLE VUNG(
    ID INT AUTO_INCREMENT PRIMARY KEY,
    TENVUNG varchar(255) NOT NULL unique,
    DIENTICH BIGINT
)

CREATE TABLE DICHVU(
    ID INT AUTO_INCREMENT PRIMARY KEY,
    TENDICHVU varchar(255) NOT NULL unique
)
USE UDM;
CREATE TABLE BANGGIA(
    ID INT AUTO_INCREMENT PRIMARY KEY,
    ID_DICHVU INT NOT NULL,
    ID_VUNG INT NOT NULL,
    GIA FLOAT 
)

ALTER TABLE BANGGIA
ADD FOREIGN KEY (ID_VUNG)
REFERENCES VUNG (ID)


ALTER TABLE BANGGIA
ADD FOREIGN KEY (ID_DICHVU)
REFERENCES DICHVU (ID)